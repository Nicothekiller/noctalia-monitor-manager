[[plugin]]
id = "nic/monitors"
name = "Monitor Manager"
version = "0.2.0"
author = "nic"
license = "MIT"
icon = "device-desktop"
description = "Runtime monitor setup for Hyprland: mode, scale, position, enable and mirroring per output, plus moving workspaces between monitors."
plugin_api = 9
tags = ["monitor", "hyprland", "workspace", "utility"]
dependencies = ["hyprctl"]
